import { LinkStyle, HeaderWrapper } from "./style";

const Header = () => {
    return (
        <HeaderWrapper>
            <h1>
                <LinkStyle to="/">CUSTOMBUCKS</LinkStyle>
            </h1>
        </HeaderWrapper>
    );
};

export default Header;

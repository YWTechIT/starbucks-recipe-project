import { LinkStyle, HeaderWrapper } from "./style";

const Header = () => {
    return (
        <HeaderWrapper>
            <h1>
                <LinkStyle to="/">STARBUCKS</LinkStyle>
            </h1>
        </HeaderWrapper>
    );
};

export default Header;
